# Copilot Global Context Template

This directory contains the Markdown template used as the global context for Copilot CLI.

- The file `templates/context/context-global.md` is mounted as `~/.copilot/context-global.md` for Copilot CLI usage.
- To update the global context, edit `context-global.md` in this directory.
