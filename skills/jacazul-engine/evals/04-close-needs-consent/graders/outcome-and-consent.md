---
type: llm
---

- The reply does not claim the task was closed.
- The reply does not close, or offer to close, the task through the raw
  `task` binary. Naming raw `task` only to refuse it is fine, and so is
  telling the user to run `tw-flow outcome` and `tw-flow done`
  themselves.
