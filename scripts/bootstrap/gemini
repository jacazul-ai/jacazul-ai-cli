#!/bin/bash
# 🐊 Jacazul - Bootstrap for Gemini CLI Settings
# Configures experimental agents and preview features surgically.

# Locate the project root directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"
TEMPLATES_DIR="$PROJECT_ROOT/templates"

GEMINI_DIR="${1:-$HOME/.gemini}"
SETTINGS_FILE="$GEMINI_DIR/settings.json"
TEMPLATE_SETTINGS="$PROJECT_ROOT/templates/gemini/unhinged/settings.json"

# 1. Ensure the Gemini directory exists
if [ ! -d "$GEMINI_DIR" ]; then
    echo "🐊 Creating Gemini directory at $GEMINI_DIR..."
    mkdir -p "$GEMINI_DIR"
fi

# 2. Ensure settings.json exists (use template if available)
if [ ! -f "$SETTINGS_FILE" ]; then
    if [ -f "$TEMPLATE_SETTINGS" ]; then
        echo "🐊 Initializing Gemini settings.json from template..."
        cp "$TEMPLATE_SETTINGS" "$SETTINGS_FILE"
    else
        echo "{}" > "$SETTINGS_FILE"
    fi
fi

# 3. Surgical update with jq
# Preserves existing config and only ensures required fields are present
TMP_FILE=$(mktemp)
jq '
    .experimental.enableAgents = true | 
    .general.previewFeatures = true
' "$SETTINGS_FILE" > "$TMP_FILE" && mv "$TMP_FILE" "$SETTINGS_FILE"

# 4. Inject Gemini Policies (Surgical)
POLICIES_DIR="$GEMINI_DIR/policies"
mkdir -p "$POLICIES_DIR"

# 4.1 Sync policies from root-level /.gemini/policies directory
REPO_POLICIES="$PROJECT_ROOT/.gemini/policies"
if [ -d "$REPO_POLICIES" ]; then
    for policy_file in "$REPO_POLICIES"/*.toml; do
        [ -f "$policy_file" ] || continue
        name=$(basename "$policy_file")
        target="$POLICIES_DIR/$name"
        
        # Always ensure the home policy matches the repo version
        if [ -n "$DEBUG" ]; then
            echo "🐊 Syncing Gemini policy: $name"
        fi
        cp "$policy_file" "$target"
    done
fi

# 5. Link Global Skills (Surgical)
SKILLS_DIR="$GEMINI_DIR/skills"
mkdir -p "$SKILLS_DIR"

# 5.1 Link skills from root-level /skills directory
for skill_dir in "$PROJECT_ROOT/skills"/*; do
    [ -d "$skill_dir" ] || continue
    name=$(basename "$skill_dir")

    target="$SKILLS_DIR/$name"
    # Always ensure the link points to the CURRENT PROJECT_ROOT
    if [ -L "$target" ]; then
        current_target=$(readlink -f "$target")
        expected_target=$(readlink -f "$skill_dir")
        if [ "$current_target" != "$expected_target" ]; then
            if [ -n "$DEBUG" ]; then
                echo "⚠️  Gemini skill $name points to wrong location. Re-linking..."
            fi
            ln -sf "$skill_dir" "$target"
        fi
    else
        if [ -n "$DEBUG" ]; then
            echo "🐊 Linking global Gemini skill: $name"
        fi
        ln -sf "$skill_dir" "$target"
    fi
done

# 5.2 Link legacy skills from templates (if any left)
if [ -d "$TEMPLATES_DIR/skills" ]; then
    for skill_dir in "$TEMPLATES_DIR/skills"/*; do
        [ -d "$skill_dir" ] || continue
        name=$(basename "$skill_dir")

        # Skip if already linked from root
        [ -e "$SKILLS_DIR/$name" ] && continue

        target="$SKILLS_DIR/$name"
        if [ -n "$DEBUG" ]; then
            echo "🐊 Linking legacy Gemini skill from templates: $name"
        fi
        ln -sf "$skill_dir" "$target"
    done
fi

# Log only in DEBUG mode
if [ -n "$DEBUG" ]; then
    echo "✅ Jacazul: Gemini configuration verified (enableAgents: true, previewFeatures: true)."
fi
